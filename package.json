{
  "name": "ctn-research-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "postbuild": "pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro",
    "harvest:pubmed": "node scripts/harvest-pubmed.mjs",
    "harvest:trials": "node scripts/sync-trials.mjs",
    "harvest:all": "npm run harvest:pubmed && npm run harvest:trials"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "pagefind": "^1.1.0"
  },
  "engines": {
    "node": ">=22"
  }
}
